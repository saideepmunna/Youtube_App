import React from "react";
import MainContainer from "./MainContainer";
import SideMenu from "./SideMenu";

const Body = () => {
  return (
    <div>
      <SideMenu />
      <MainContainer />
    </div>
  );
};

export default Body;
